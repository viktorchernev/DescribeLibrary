utilizing -> 
	
	basics <B5u0wAmQ> ->

		// Conveniently auto escape '*', '\', '/', '-', 
		// so that the used does not have to
		auto escapes <BQAfF2EN>,
		
		// When parsing folders, compiler will parse
		// only .DS files, and ignore others
	*	files .ds only <QML9pYXu>,
	
		// Compiler will try to parse draft .DR files as well.
		// Those are ignored by default
	*	files .dr too <OL6pMWkD>;
		
	tags ->
	
		basics <B5u0wAmQ>;


LEGEND ->

	! - Has name,
	~ - Is unplugable,
	* - Is plugable;